---
description: Sync research and plan documents to ~/.planning for cross-project access.
model: sonnet
---

You are tasked with syncing planning documents to the centralized ~/.planning directory.

Load and follow the protocol in the autodidact-sync-thoughts skill.

If the user provided a file path after `/sync-thoughts`, sync only that file. Otherwise, sync all research and plan documents from the current project's `.planning/` directory.
